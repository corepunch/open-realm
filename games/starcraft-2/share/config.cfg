// StarCraft II gameplay defaults (installed to build/share/starcraft-2/).
// User overrides are written to ~/.starcraft-2/config.cfg.

seta vid_mode "2"

bind MOUSE1 "+select"
bind MOUSE2 "+smart"
bind MOUSE3 "+pan"
bind ALT+MOUSE1 "+pan"
bind ESCAPE "cmd cancel"
bind F10 "screenshot"
