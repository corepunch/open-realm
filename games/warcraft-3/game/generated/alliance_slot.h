/* Generated by fdfbindgen. Do not edit by hand. */
#ifndef ALLIANCESLOT_H
#define ALLIANCESLOT_H

#include "../g_local.h"

typedef struct AllianceSlot_s {
    frameDef_t *AllianceSlot;
    frameDef_t *ColorBackdrop;
    frameDef_t *ColorBorder;
    frameDef_t *PlayerNameLabel;
    frameDef_t *AllyCheckBox;
    frameDef_t *VisionCheckBox;
    frameDef_t *UnitsCheckBox;
    frameDef_t *GoldBackdrop;
    frameDef_t *GoldText;
    frameDef_t *LumberBackdrop;
    frameDef_t *LumberText;
} AllianceSlot_t;


static inline bool AllianceSlot_Bind(AllianceSlot_t *out, frameDef_t *bind_root) {
    bool ok = true;
    if (!out) {
        return false;
    }
    memset(out, 0, sizeof(*out));
    out->AllianceSlot = bind_root;
    if (!out->AllianceSlot) {
        BZ_FDF_REPORT_MISSING("AllianceSlot");
        ok = false;
    }
    BZ_FDF_BIND_CHILD(out, ColorBackdrop, bind_root, "ColorBackdrop");
    BZ_FDF_BIND_CHILD(out, ColorBorder, out->ColorBackdrop, "ColorBorder");
    BZ_FDF_BIND_CHILD(out, PlayerNameLabel, bind_root, "PlayerNameLabel");
    BZ_FDF_BIND_CHILD(out, AllyCheckBox, bind_root, "AllyCheckBox");
    BZ_FDF_BIND_CHILD(out, VisionCheckBox, bind_root, "VisionCheckBox");
    BZ_FDF_BIND_CHILD(out, UnitsCheckBox, bind_root, "UnitsCheckBox");
    BZ_FDF_BIND_CHILD(out, GoldBackdrop, bind_root, "GoldBackdrop");
    BZ_FDF_BIND_CHILD(out, GoldText, out->GoldBackdrop, "GoldText");
    BZ_FDF_BIND_CHILD(out, LumberBackdrop, bind_root, "LumberBackdrop");
    BZ_FDF_BIND_CHILD(out, LumberText, out->LumberBackdrop, "LumberText");
    return ok;
}

static inline bool AllianceSlot_Load(AllianceSlot_t *out) {
    return out &&
           UI_EnsureFDF("UI\\FrameDef\\UI\\AllianceSlot.fdf") &&
           AllianceSlot_Bind(out, UI_FindFrame("AllianceSlot"));
}

#endif /* ALLIANCESLOT_H */
